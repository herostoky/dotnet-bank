using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;
using System.Windows.Forms;

namespace bank
{
    static class Program
    {
        /// <summary>
        /// The main entry point for the application.
        /// </summary>
        [STAThread]
        static void Main()
        {
            //Application.EnableVisualStyles();
            //Application.SetCompatibleTextRenderingDefault(false);
            //Application.Run(new Form1());

            //double amount = 10_000_000;
            //int durationInMonth = 12;
            //int periodicity = 1;
            //double valueWitoutRate = services.BankService.getAmountPerPeriod(amount, durationInMonth, periodicity);
            //double value = services.BankService.getAmountPerPeriodWithRate(amount, durationInMonth, periodicity);
        }
    }
}
